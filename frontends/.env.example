# Dev: cổng backend Next.js
VITE_API_PORT=3000

# Production (VPS): để trống — dùng cùng domain với Nginx
# VITE_API_URL=

# Chấm công — danh sách WiFi lấy từ API / database (CompanyWifi), không cần cấu hình frontend
